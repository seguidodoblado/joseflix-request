# Instalación

```bash
sudo apt update
sudo apt install python3 python3-gi gir1.2-gtk-4.0
./build-deb.sh
sudo apt install ../joseflix-request_1.0.0-1_all.deb
```

Rutas instaladas:

```text
/usr/bin/joseflix-request
/usr/share/joseflix-request/
/opt/joseflix-request/
```
