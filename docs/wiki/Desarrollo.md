# Desarrollo

La aplicación utiliza Python 3, GTK 4, PyGObject, SQLite y la API de TMDB.

La versión del paquete se obtiene de la primera entrada de `debian/changelog`. Para generar el `.deb`:

```bash
./build-deb.sh
```

El paquete se escribe fuera de la carpeta del proyecto.
