# Configuración de TMDB

Abre **Ajustes → Configurar TMDB…**, introduce el token de acceso de lectura de TMDB y pulsa **Guardar**.

El token se guarda localmente en:

```text
~/.local/share/joseflix-request/config.json
```

No publiques ese archivo ni el token en el repositorio.
